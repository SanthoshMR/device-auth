device-auth is a SailsJS authentication system based on Waterlock. Since Waterlock is mainly targeted towards users, this repo uses it authenticate devices by changing its models to point to devices.
